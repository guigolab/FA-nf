manifest.mainScript = 'pipeline.nf'

docker.enabled = true
docker.runOptions = "-v /blast-db:/data"

process {
           beforeScript = "module purge"

}

profile {
  standard {
    includeConfig params.config
  }
}